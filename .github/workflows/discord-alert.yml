name: Discord Alert

# 리뷰 요청, 리뷰 시 발동
on:
  pull_request:
    types: [ review_requested ]
  pull_request_review:
    types: [ submitted ]

jobs:
  notify:
    name: 알림보내기
    runs-on: ubuntu-latest
    steps:
      - name: GitHub ID와 Discord ID 매핑
        id: mapper
        shell: bash
        run: |
          if [[ "${{ github.event.action }}" == "review_requested" ]]; then
            TARGET_GITHUB_ID="${{ github.event.requested_reviewer.login }}"
          else
            TARGET_GITHUB_ID="${{ github.event.pull_request.user.login }}"
          fi
          
          # GitHub ID(오른쪽)에 따라 Discord ID(왼쪽)를 설정
          case "$TARGET_GITHUB_ID" in
            "Dompoo") DISCORD_ID="@dom_poo" ;;
            "doabletuple") DISCORD_ID="@doabletuple" ;;
            "wondroid-world") DISCORD_ID="@wondroid_world" ;;
            "kkiseug") DISCORD_ID="@kksuk" ;;
            "giovannijunseokim") DISCORD_ID="@giovannijunseokim" ;;
            "vvn89") DISCORD_ID="@tobae.time" ;;
            *) DISCORD_ID="$TARGET_GITHUB_ID" ;; # 목록에 없으면 GitHub 아이디 그대로 사용
          esac
          
          # 3) 결과 저장
          echo "MENTION_TARGET=$DISCORD_ID" >> $GITHUB_ENV
      - name: 리뷰 요청 알림
        if: github.event.action == 'review_requested'
        uses: Ilshidur/action-discord@master
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        with:
          args: |
            ―――――――――――――――――――――――――
            🙇‍♂️ **리뷰 요청**
            🎯 {{ EVENT_PAYLOAD.sender.login }} → ${{ env.MENTION_TARGET }}
            🔗 [{{ EVENT_PAYLOAD.pull_request.title }}](<{{ EVENT_PAYLOAD.pull_request.html_url }}>)

      - name: 리뷰 완료 알림
        if: github.event_name == 'pull_request_review'
        uses: Ilshidur/action-discord@master
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        with:
          args: |
            ―――――――――――――――――――――――――
            ✅ **리뷰 등록**
            🎯 {{ EVENT_PAYLOAD.sender.login }} → ${{ env.MENTION_TARGET }}
            🔗 [{{ EVENT_PAYLOAD.pull_request.title }}](<{{ EVENT_PAYLOAD.review.html_url }}>)
