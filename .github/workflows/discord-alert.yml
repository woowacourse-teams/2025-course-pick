name: Discord Alert

# 리뷰 요청, 리뷰 시 발동
on:
  pull_request:
    types: [ review_requested ]
  pull_request_review:
    types: [ submitted ]

jobs:
  notify:
    name: 알림보내기
    runs-on: ubuntu-latest
    steps:
      - name: 리뷰 요청 알림
        if: github.event.action == 'review_requested'
        uses: Ilshidur/action-discord@master
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        with:
          args: |
            ***
            📢 **리뷰 요청**
            🎯 {{ EVENT_PAYLOAD.sender.login }} -> {{ EVENT_PAYLOAD.requested_reviewer.login }}
            🔗 [{{ EVENT_PAYLOAD.pull_request.title }}](<{{ EVENT_PAYLOAD.pull_request.html_url }}>)

      - name: 리뷰 완료 알림
        if: github.event_name == 'pull_request_review'
        uses: Ilshidur/action-discord@master
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        with:
          args: |
            ***
            ✅ **리뷰 등록**
            🎯 {{ EVENT_PAYLOAD.sender.login }} -> {{ EVENT_PAYLOAD.pull_request.user.login }}
            🔗 [{{ EVENT_PAYLOAD.pull_request.title }}](<{{ EVENT_PAYLOAD.review.html_url }}>)
