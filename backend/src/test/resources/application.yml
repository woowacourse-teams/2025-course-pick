spring:
  datasource:
    url: jdbc:h2:mem:test-coursepick;MODE=MySQL

  data:
    mongodb:
      auto-index-creation: true

de.flapdoodle.mongodb.embedded.version: 8.0.12

admin:
  token: secret
  kakao-map-api-key: secret

notice:
  base-url: http://localhost:8080/images
