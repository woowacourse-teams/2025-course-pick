de:
  flapdoodle:
    mongodb:
      embedded:
        version: 8.0.0

spring:
  datasource:
    url: jdbc:h2:mem:test-coursepick;MODE=MySQL
  data:
    mongodb:
      port: 0
      database: test

admin.token: secret
