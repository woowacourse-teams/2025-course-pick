spring:
  config.activate.on-profile: local
  data.mongodb:
    uri: mongodb://root:secret@localhost:27017/coursepick?authSource=admin
  datasource:
    url: jdbc:mysql://localhost:3307/coursepick
    username: user
    password: secret
  batch:
    jdbc.initialize-schema: always
    job.enabled: false

admin.token: secret
